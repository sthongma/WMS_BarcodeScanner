# ===================================================================
# WMS Barcode Scanner - Environment Variables Template
# ===================================================================
# Copy this file to .env and fill in your actual values
# Usage: cp .env.example .env (Linux/Mac) or copy .env.example .env (Windows)
# ===================================================================

# ===================================================================
# Database Configuration (SQL Server)
# ===================================================================
# Connection string will be built from these variables

# SQL Server hostname or IP address
DB_SERVER=localhost

# Database name
DB_DATABASE=WMS_BarcodeScanner

# Authentication type: 'Windows' or 'SQL'
# Note: Windows authentication only works on Windows hosts
DB_AUTH_TYPE=SQL

# SQL Authentication credentials (required if AUTH_TYPE=SQL)
DB_USERNAME=sa
DB_PASSWORD=YourStrong@Passw0rd

# ODBC Driver name (default: ODBC Driver 17 for SQL Server)
# Change this if you have a different version installed
DB_DRIVER=ODBC Driver 17 for SQL Server

# ===================================================================
# Flask Application Configuration
# ===================================================================

# Flask environment: 'development' or 'production'
FLASK_ENV=development

# Enable/disable debug mode (1=enable, 0=disable)
# WARNING: Never enable debug mode in production!
FLASK_DEBUG=1

# Host to bind to (0.0.0.0 = all interfaces)
FLASK_HOST=0.0.0.0

# Port to listen on
FLASK_PORT=5000

# Secret key for Flask sessions (generate a random string for production)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=dev-secret-key-change-this-in-production

# ===================================================================
# Logging Configuration
# ===================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log directory (relative to project root)
LOG_DIR=logs

# Enable file logging (1=enable, 0=disable)
LOG_TO_FILE=1

# Enable console logging (1=enable, 0=disable)
LOG_TO_CONSOLE=1

# ===================================================================
# Application Configuration
# ===================================================================

# Application timezone (e.g., Asia/Bangkok, UTC)
TZ=Asia/Bangkok

# Maximum upload file size in MB
MAX_UPLOAD_SIZE=10

# Session timeout in minutes
SESSION_TIMEOUT=30

# ===================================================================
# CORS Configuration (Cross-Origin Resource Sharing)
# ===================================================================

# Allowed origins for CORS (comma-separated list)
# Use * for development, specific domains for production
CORS_ORIGINS=*

# ===================================================================
# Optional: Development Tools
# ===================================================================

# Enable SQL query logging (1=enable, 0=disable)
# Useful for debugging database queries
DEBUG_SQL=0

# Enable request/response logging (1=enable, 0=disable)
DEBUG_REQUESTS=0

# ===================================================================
# Optional: SQL Server Container (if using Docker SQL Server)
# ===================================================================
# Uncomment these if you want to run SQL Server in Docker

# ACCEPT_EULA=Y
# SA_PASSWORD=YourStrong@Passw0rd
# MSSQL_PID=Developer

# ===================================================================
# Notes:
# ===================================================================
# 1. Never commit the .env file to version control
# 2. Generate strong passwords for production
# 3. Use secrets management tools (e.g., Docker secrets, Azure Key Vault) in production
# 4. For Windows authentication in Docker, you need to configure additional settings
# 5. Make sure DB_SERVER is accessible from the Docker container
#    - Use 'host.docker.internal' on Windows/Mac to access host machine
#    - Use host IP address on Linux
