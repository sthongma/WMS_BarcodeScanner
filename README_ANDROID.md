# 📱 WMS Barcode Scanner สำหรับ Android

## 🚀 วิธีใช้งานง่ายๆ บน Android

### 📋 ความต้องการ
- เครื่องคอมพิวเตอร์ที่รันโปรแกรม (Windows/Linux/Mac)
- โทรศัพท์ Android ที่เชื่อมต่อ WiFi เดียวกัน
- เว็บเบราว์เซอร์บน Android (Chrome, Firefox, Safari)

### 🔧 การติดตั้ง

1. **ติดตั้ง Dependencies:**
   ```bash
   pip install -r requirements.txt
   ```

2. **รัน Web Application:**
   ```bash
   python web_app.py
   ```

3. **ดู IP Address ของเครื่อง:**
   - Windows: เปิด Command Prompt แล้วพิมพ์ `ipconfig`
   - Linux/Mac: เปิด Terminal แล้วพิมพ์ `ifconfig` หรือ `ip addr`

### 📱 การใช้งานบน Android

1. **เปิดเว็บเบราว์เซอร์บน Android**

2. **เข้าไปที่ URL:**
   ```
   http://[IP_ADDRESS]:5000
   ```
   ตัวอย่าง: `http://192.168.1.100:5000`

3. **เข้าสู่ระบบ:**
   - กรอกข้อมูล Server, Database, Username, Password
   - กดปุ่ม "เข้าสู่ระบบ"

4. **เริ่มสแกน:**
   - เลือก Job Type และ Sub Job Type
   - พิมพ์บาร์โค้ดหรือใช้กล้องสแกน
   - กดปุ่ม "สแกน"

### 🎯 ฟีเจอร์หลัก

- ✅ **Responsive Design** - ใช้งานได้ดีบนมือถือ
- ✅ **Camera Access** - ใช้กล้องมือถือสแกนบาร์โค้ด
- ✅ **Real-time History** - ดูประวัติการสแกนแบบ Real-time
- ✅ **Duplicate Detection** - ตรวจสอบบาร์โค้ดซ้ำ
- ✅ **Offline Support** - ทำงานได้แม้ไม่มีอินเทอร์เน็ต (ในเครือข่ายท้องถิ่น)

### 🔧 การตั้งค่าขั้นสูง

#### การเปิดใช้งาน HTTPS (แนะนำสำหรับการใช้งานจริง)
```bash
# สร้าง SSL certificate
openssl req -x509 -newkey rsa:4096 -nodes -out cert.pem -keyout key.pem -days 365

# รันด้วย HTTPS
python web_app.py --ssl
```

#### การตั้งค่า Firewall
- เปิด Port 5000 ใน Windows Firewall
- อนุญาตการเชื่อมต่อจากเครือข่ายท้องถิ่น

### 🛠️ การแก้ไขปัญหา

#### ไม่สามารถเชื่อมต่อได้
1. ตรวจสอบว่า Android และคอมพิวเตอร์อยู่ใน WiFi เดียวกัน
2. ตรวจสอบ IP Address ว่าถูกต้อง
3. ตรวจสอบ Firewall settings

#### กล้องไม่ทำงาน
1. ตรวจสอบว่าเว็บไซต์ใช้ HTTPS (กล้องต้องการ HTTPS)
2. อนุญาตการเข้าถึงกล้องในเบราว์เซอร์
3. ลองรีเฟรชหน้าเว็บ

#### การเชื่อมต่อฐานข้อมูลล้มเหลว
1. ตรวจสอบข้อมูลการเชื่อมต่อ
2. ตรวจสอบว่า SQL Server อนุญาตการเชื่อมต่อจากภายนอก
3. ตรวจสอบ Network Configuration ของ SQL Server

### 📊 การใช้งานแบบ Advanced

#### การใช้งานผ่าน QR Code
1. สร้าง QR Code ที่มี URL ของแอปพลิเคชัน
2. สแกน QR Code ด้วยมือถือเพื่อเข้าถึงแอป

#### การใช้งานแบบ PWA (Progressive Web App)
1. เพิ่มไฟล์ `manifest.json` และ `service-worker.js`
2. ผู้ใช้สามารถ "ติดตั้ง" แอปบนมือถือได้

### 🔒 ความปลอดภัย

- ใช้ HTTPS สำหรับการใช้งานจริง
- ตั้งค่า Authentication ที่แข็งแกร่ง
- จำกัดการเข้าถึงจาก IP ที่อนุญาต
- ใช้ Environment Variables สำหรับข้อมูลที่สำคัญ

### 📞 การสนับสนุน

หากมีปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ Log ใน Console ของเบราว์เซอร์
2. ตรวจสอบ Log ของ Python Application
3. ตรวจสอบการเชื่อมต่อเครือข่าย

---

**🎉 ตอนนี้คุณสามารถใช้ WMS Barcode Scanner บน Android ได้แล้ว!** 